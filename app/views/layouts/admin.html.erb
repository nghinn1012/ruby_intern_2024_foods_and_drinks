<!DOCTYPE html>
<html>
  <head>
    <title><%= t("admin.title") %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "admin", "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <%= render "layouts/admin/sidebar" %>
    <div class="wrapper d-flex flex-column min-vh-100 bg-light m-0">
      <%= render "layouts/admin/header" %>
      <div class="body flex-grow-1 mt-0">
        <div class="container-lg mt-0">
          <div class="container wrap mt-0">
            <%= render "shared/messages" %>
            <%= yield %>
          </div>
        </div>
        <%= render "layouts/admin/footer" %>
      </div>
    </div>
  </body>
</html>
